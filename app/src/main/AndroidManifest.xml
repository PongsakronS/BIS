<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.toeypza.dashboardmenu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Pregnancy_History"
            android:theme="@style/ThemePregnancy_history" />
        <activity
            android:name=".Fetal_examination"
            android:theme="@style/ThemeFetal_examination" />
        <activity
            android:name=".Disease"
            android:theme="@style/ThemeDisease" />
        <activity
            android:name=".Counting_balls"
            android:theme="@style/ThemeCounting_balls" />
        <activity
            android:name=".Check_the_pregnancy"
            android:theme="@style/ThemeCheck_the_pregnancy" />
        <activity
            android:name=".manipulator"
            android:theme="@style/ThemeManipulator"/>
        <activity
            android:name=".Free"
            android:theme="@style/ThemeFree">
        </activity>
    </application>

</manifest>